   q1, q2, q3, q4, q5
u1 10, 14, 13, , 80


- Content-based: trova similarità tra le varie query

X classe classificatore
X trovare gli answers' set delle query

- trattare gli answers' set come documenti
- min-hash: per ogni query, vedere quale persona compare e quale no (1 o 0)
- jaccard similarity (intersezione / unione, ma condizionata dalla similirità degli attributi (John, Johnny))
- hash function per mappare una tupla in un set limitato di risposte, utilizzato poi per il calcolo della similarità
- calcolare la similarità tra i vari insiemi (cosine similarity) -> compara le persone del primo set con il secondo e prendere la media delle similarità più alte
- la similarità si calcola tramite vettori
- convertire i dati in vettori?


# BIG IDEA
Convertire l'answer set in plain text, lsh per trovare i candidate pairs